# DevOps / Terraform (Azure)

This repository is for my project setup and testing.

Current status:
- `dev` is only a test environment for now.
- only `terraform/environments/dev` is tracked right now.
